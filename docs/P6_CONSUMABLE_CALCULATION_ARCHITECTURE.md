# P6 焊材定额第 1-3 部分实现边界

## 分层与语义映射

- `app.consumable_calculation` 是纯函数包，只依赖 Python 标准库，不导入 FastAPI、SQLAlchemy、文件、网络、系统时间或参数库。
- `ConsumableCalculationService` 只负责把关系化记录映射为不可变输入，并把完整中间结果交回持久层；公式不放在 Service。
- `ConsumableGeometryInput` 以 `ProductRevision → WeldJoint → WeldSequenceRevision/WeldSequenceStep` 为追溯主线，替代桌面项目的 `QuoteProject/WeldTask`。
- `WeldConsumableOperation` 每道工序一行，方法、材料、面积、长度和效率均可查询、可版本化。JSONB 只保存输入与结果快照，不承载多工序主结构。

## 材料定额与报价边界

本阶段只计算理论几何量、熔敷质量、主焊材、焊剂、气体和时间中间量。纯函数输入和持久化模型都不包含单价、人工费、利润、税率或报价字段。后续报价能力只能消费已批准的定额结果，不能反向改变定额公式。

## 几何与长度安全规则

- 正面填充、清根槽、背面余高分别计算，填充系数只作为小裕量统一展示。
- 清根默认要求显式槽口宽度；`reference_trapezoid` 只是可选参考近似，并始终返回工程师确认警告。
- 环缝必须明确内径、中径或外径，并保存包角和数量。
- 图纸解析来源只有在工程师确认后才能进入计算。
- 生产默认参数库尚未在第 1-3 部分启用；参考项目默认值不能自动成为生产默认值。

## 单位与公式

- 长度：`mm`
- 面积：`mm²`
- 密度：`g/cm³`
- 质量：`kg`
- 时间：`h`
- 熔敷质量：`面积 × 长度 × 密度 ÷ 1,000,000`
- 主焊材：`熔敷质量 ÷ 熔敷效率`
- SAW 焊剂：`主焊材 × 焊剂/焊丝配比`
- 气体：`流量(L/min) × 电弧时间(h) × 60`

焊道数只保留为工序描述，质量计算的固定乘数为 `1.0`，避免对已代表完整熔敷截面的面积再次重复相乘。

## 多方法与多工序

- 工序角色固定为正面填充、清根填充、固定焊和自定义工序。
- 绑定坡口的工序必须明确分配正面或清根面积，同一面积来源的分配比例合计必须为 `1.0`；固定焊和自定义工序使用独立确认面积。
- 每道工序独立冻结方法、焊材、效率、熔敷速度、燃弧系数、焊剂配比、气体流量、损耗规则和包装规则。
- 汇总分别累计熔敷量、主焊材、焊剂、气体、电弧时间和总工时。缺损、已废止或不完整工序直接报错，不回退为单工序。
- 旧单工序数据只能通过显式兼容转换进入 `custom` 角色，并生成迁移审计和工程师复核警告。

## 企业参数与历史定额

- 焊接方法参数、典型坡口预设和企业规则均具有个人/企业/工厂作用域、版本、生效区间、审批状态、启停状态和前序版本。
- 已被正式定额引用的参数保留历史版本，只能停用或创建新版本；参数变化不会改变历史快照。
- 定额结果分层保存理论熔敷量、工艺消耗量、企业定额量、包装取整量和建议领用量，并标明系统计算、企业配置或人工覆盖来源。
- 每次运行冻结公式、几何、长度、工序、方法、焊材和企业规则，使用规范化哈希构造幂等键。
- 人工覆盖必须填写原因并形成待审核记录。主动重算生成新版本和字段差异，不覆盖已批准或已下发结果。
- 坡口、长度、方法、焊材或企业系数变化会把关联运行标记为过期；未经工程校核的清根近似公式不能进入正式定额。

## 领用清单与库存边界

- 只有已批准的定额运行可以生成领用建议；生成动作冻结库存快照和缺口，但绝不修改库存。
- 产品汇总按固体焊材、焊剂和保护气体分类，并以牌号、规格、批次要求、工厂和单位作为分组键。SAW 焊丝与焊剂始终分行，气体使用 `L`，固体和焊剂使用 `kg`。
- 每行保留产品版本、焊序版本、焊缝、焊序步骤、定额工序、方法参数、焊材参数和结果快照。库存不足只显示缺口，不替换牌号、规格、批次或工厂。
- 清单建议批准后，实际领用才创建 `out` 库存事务并扣减库存；退料创建 `return` 事务并恢复库存。实际消耗在领用范围内单独记录，不重复扣减库存。
- 实际事件使用幂等键，并校验单位、批次、工序追溯、库存和“消耗不超过净领用”。偏差报告只生成校准建议，明确禁止自动修改已批准企业系数。
- 支持 UTF-8 CSV 导出焊缝定额明细、产品汇总和正式领用清单；文本字段执行电子表格公式注入防护。
